on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-python-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install feedparser matplotlib nltk

    - name: Download NLTK data
      run: |
        python -m nltk.downloader -d ./nltk_data punkt punkt_tab wordnet averaged_perceptron_tagger averaged_perceptron_tagger_eng
    - name: Run main.py
      env:
        NLTK_DATA: ./nltk_data
      run: python main.py
